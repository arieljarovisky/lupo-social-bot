{
  "name": "lupo-social-bot",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Bot propio para DM y comentarios de Instagram/Facebook de Lupo Argentina",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch --env-file=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
